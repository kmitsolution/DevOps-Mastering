apiVersion: v1
kind: Pod
metadata:
  name: frontend-pod-soft
spec:
  affinity:
    podAffinity:
      preferredDuringSchedulingIgnoredDuringExecution:
      - weight: 100
        podAffinityTerm:
          labelSelector:
            matchExpressions:
            - key: app
              operator: In
              values:
              - backend1
          topologyKey: kubernetes.io/hostname
  containers:
  - name: nginx
    image: nginx
