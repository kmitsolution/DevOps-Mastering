## 📝 Git Hands-On Quiz (Lecture 04)

### **Q1.** Which command is used to create a new local Git repository?
```
a) git clone
b) git start
c) git init
d) git create
```
---

### **Q2.** After running `git init`, which command helps you check the current state of files?
```
a) git log
b) git status
c) git diff
d) git show
```
---

### **Q3.** Which command stages a single file named `bal.java`?
```
a) git add .
b) git add bal.java
c) git stage bal.java
d) git commit bal.java
```
---

### **Q4.** What does the command `git rm --cached bal.java` do?
```
a) Deletes the file permanently
b) Removes the file from working directory
c) Removes the file from staging area only
d) Deletes the repository
```
---

### **Q5.** Which command stages **all modified and new files** at once?
```
a) git add *
b) git add -A
c) git add --cached
d) git stage all
```
---

### **Q6.** Why is `git commit` called a **repository snapshot**?
```
a) It deletes old files
b) It copies files to GitHub
c) It saves the current state of staged files
d) It compresses files
```
---

### **Q7.** Which two configurations are mandatory before creating a commit?
```
a) Repository name & branch
b) Username & password
c) Author name & email
d) Remote URL & token
```
---

### **Q8.** Which command sets the **global Git username**?
```
a) git config user.name globaluser
b) git config --local user.name globaluser
c) git config --global user.name globaluser
d) git set global user.name
```
---

### **Q9.** Where does **local Git configuration** apply?
```
a) Entire system
b) All repositories of the user
c) Only the current repository
d) Remote repository
```
---

### **Q10.** Which command shows the commit history in **single-line format**?
```
a) git log
b) git log --oneline
c) git status
d) git show
```
---

## ✅ **Answer Key**

1. c) git init
2. b) git status
3. b) git add bal.java
4. c) Removes the file from staging area only
5. b) git add -A
6. c) It saves the current state of staged files
7. c) Author name & email
8. c) git config --global user.name globaluser
9. c) Only the current repository
10. b) git log --oneline

---

